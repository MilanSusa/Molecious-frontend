import React from "react";
import Logo from "../Logo/Logo";

const Home = props => {
    return (
        <React.Fragment>
            <Logo/>
        </React.Fragment>
    );
};

export default Home;